# Pin LiteLLM to a version that installs reliably on this Windows environment.
litellm==1.61.15
python-dotenv>=1.0.0
pypdf>=4.0.0
# Optional — uncomment if you add a Streamlit UI (stretch goal):
streamlit>=1.30.0
google-genai
